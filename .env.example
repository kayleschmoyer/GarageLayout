# Google OAuth 2.0 Configuration
# Create credentials at: https://console.cloud.google.com/apis/credentials
#
# Required Google Cloud Console setup for the OAuth 2.0 Client ID:
#   1. Application type: Web application
#   2. Authorized JavaScript Origins: add your app's origin (e.g. http://localhost:5173)
#   3. Authorized Redirect URIs: add your app's origin (same as above)
#
# The "redirect_uri_mismatch" error means the origin where the app is running
# is not listed in the Authorized JavaScript Origins for this client ID.

VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_GOOGLE_SHARED_FOLDER_ID=your-shared-drive-folder-id
